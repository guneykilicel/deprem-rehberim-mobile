<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="Ana Sayfa"
            android:theme="@style/AppTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DepremBilgilendirmeActivity"
            android:label="Deprem Bilgilendirme"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".AcilDurumPlaniActivity"
            android:label="Acil Durum Planı"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".DepremHazirliklariActivity"
            android:label="Deprem Öncesi Hazırlık"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".DepremSirasindaActivity"
            android:label="Deprem Sırasında Yapılacaklar"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".DepremSonrasiActivity"
            android:label="Deprem Sonrası İlk Yardım"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ComplaintOrMessageActivity"
            android:label="Şikayet ve İleti"
            android:theme="@style/AppTheme" />

        <!-- Diğer aktiviteleri buraya ekleyebilirsiniz -->

    </application>

</manifest>